name: fluffypawsm
description: "Store Manager App for FluffyPaw"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.5.3

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_form_builder: ^9.5.0






  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  dartz: ^0.10.1
  equatable: ^2.0.5
  http: ^1.2.2
  flutter_riverpod: ^2.5.1
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.10+1
  intl: ^0.19.0
  loading_animation_widget: ^1.2.1
  flutter_secure_storage: ^9.2.2
  build_runner: ^2.4.0
  localization: ^2.1.1
  intl_translation: ^0.20.0
  form_builder_validators: ^11.0.0
  gap: ^3.0.1
  hive_flutter: ^1.1.0
  flutter_spinkit: ^5.2.1
  jwt_decoder: ^2.0.1
  cached_network_image: ^3.4.1
  flutter_switch: ^0.3.2
  photo_manager: ^3.6.3
  fl_chart: ^0.69.2
  carousel_slider: ^5.0.0



#  firebase_core: ^3.5.0
#  firebase_auth: ^5.3.0
  encrypt: ^5.0.3
  logging: ^1.3.0
  image_picker: ^1.1.2
  flutter_gen_runner: ^5.8.0
  flutter_animate: ^4.5.0
  logger: ^2.5.0
  flutter_slidable: ^3.0.1
  overlay_support: ^2.1.0
  flutter_screenutil: ^5.9.3
  page_transition: ^2.1.0
  intl_utils: ^2.8.7
  flutter_staggered_animations: ^1.1.1
  scrollable_positioned_list: ^0.3.8
  shimmer: ^3.0.0
  
  dio: ^5.7.0
  pretty_dio_logger: ^1.4.0
  permission_handler: ^11.3.1
  synchronized: ^3.3.0+3
  mobile_scanner: ^6.0.2
  riverpod: ^2.6.1
  flutter_background_service: ^5.0.10
  flutter_local_notifications: ^18.0.1
  flutter_background_service_android: ^6.2.7
  shared_preferences: ^2.3.3
  timeago: ^3.7.0
  app_settings: ^5.1.1
  event_bus: ^2.0.1
  firebase_core: ^3.7.0
  firebase_auth: ^5.3.2
  signalr_core: ^1.1.2
  lottie: ^3.1.3
  pinput: ^5.0.0
  animate_do: ^3.3.4
  #web_socket_channel: ^3.0.1
  lite_rolling_switch:
        git:
            url: https://github.com/herowws/lite_rolling_switch
dependency_overrides:
  web: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/svg/fluffypaw_logo.svg
    - assets/gif/L0OSegj9t4wzZwLwyD.gif
    - assets/svg/fluffy_paw_darl.svg
    - assets/svg/notification.svg
    - assets/svg/summery_bag.svg
    - assets/svg/summery_ongoing_order.svg
    - assets/svg/doller.svg
    - assets/svg/total_doller.svg
    - assets/svg/active_bag.svg
    - assets/svg/active_dashboard.svg
    - assets/svg/active_profile.svg
    - assets/svg/active_rider.svg
    - assets/svg/bag.svg
    - assets/svg/dashboard.svg
    - assets/svg/rider.svg
    - assets/svg/profile.svg
    - assets/svg/seller_profile.svg
    - assets/svg/earning_history.svg
    - assets/svg/language.svg
    - assets/svg/sun.svg
    - assets/svg/seller_support.svg
    - assets/svg/terms_conditions.svg
    - assets/svg/privacy.svg
    - assets/svg/logout.svg
    - assets/svg/store_account.svg
    - assets/image/question.png
    - assets/image/alert.png
    - assets/svg/done.svg
    - assets/svg/edit.svg
    - assets/svg/communications_active.svg
    - assets/svg/communications_notactive.svg
    - assets/image/avatar.png
    - assets/image/default_image.png
    - assets/image/shop_logo.png
    - assets/image/under_review.png


flutter_intl:
  enabled: true
  use_deferred_loading: false
  arb_dir: lib/core/l10n
  output_dir: lib/core/generated

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
